services:
  app:
    build: .
    container_name: fabbytrack-app
    expose:
      - "4325"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=4325
    volumes:
      - fabbytrack-data:/app/data
    restart: unless-stopped

volumes:
  fabbytrack-data:
